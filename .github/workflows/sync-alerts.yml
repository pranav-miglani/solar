name: Sync Alerts (Cron)

on:
  schedule:
    # Run every 30 minutes
    - cron: '*/30 * * * *'
  workflow_dispatch:  # Allow manual trigger

jobs:
  sync-alerts:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: Trigger Alert Sync
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
          API_URL: ${{ secrets.API_URL || 'https://your-domain.com' }}
        run: |
          echo "🔄 [GitHub Actions] Triggering alert sync at $(date -u +"%Y-%m-%d %H:%M:%S UTC")"
          
          response=$(curl -s -w "\n%{http_code}" -X GET \
            -H "Authorization: Bearer ${CRON_SECRET}" \
            -H "User-Agent: GitHub Actions (sync-alerts workflow)" \
            -H "Content-Type: application/json" \
            "${API_URL}/api/cron/sync-alerts")
          
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')
          
          echo "📊 [GitHub Actions] Response Status: $http_code"
          echo "📄 [GitHub Actions] Response Body: $body"
          
          if [ "$http_code" -ge 200 ] && [ "$http_code" -lt 300 ]; then
            echo "✅ [GitHub Actions] Alert sync triggered successfully"
          else
            echo "❌ [GitHub Actions] Alert sync failed with status $http_code"
            echo "$body"
            exit 1
          fi

